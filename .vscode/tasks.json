{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "./configure && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}